Could use a scheme whereby each bin subdir has an associated command parser.
In this way, we could do the right thing for filesystem type commands
(wildcarding etc) _and_ the right thing for other sorts of commands.
