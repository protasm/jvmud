will have an emote daemon /bin/daemon/emoted.  it will store emotes in
a mapping (inherits CORE).  it will have a parser that parses the emotes
it stores.  it will have an interface for adding and removing emotes.
emotes will be extractable to text files which can be edited and resubmitted
to the emote daemon (no more editing a huge file of emotes).
