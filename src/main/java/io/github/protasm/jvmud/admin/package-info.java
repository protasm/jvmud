/**
 * Administrative command interpretation and per-administrator session state.
 *
 * <p>Sessions perform operations through instance and compiler APIs. Transport delivers
 * command lines and returns output; the CLI client owns terminal interaction.</p>
 */
package io.github.protasm.jvmud.admin;
