/**
 * JDBC-backed persistence adapters.
 *
 * <p>This package owns database-facing runtime services, including SQL storage helpers exposed to
 * generated LPC code through the runtime context.</p>
 */
package io.github.protasm.jvmud.persistence.jdbc;
