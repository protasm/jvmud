#!/usr/bin/env sh
set -eu

SCRIPT_DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
ROOT_DIR=$(dirname -- "$SCRIPT_DIR")
cd "$ROOT_DIR"

mvn -q compile dependency:build-classpath \
  -Dmdep.outputFile=target/jvmud-transport-classpath.txt >/dev/null

CP="target/classes:$(cat target/jvmud-transport-classpath.txt)"
exec java -cp "$CP" io.github.protasm.jvmud.transport.telnet.TelnetServer "$@"
