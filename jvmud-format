#!/usr/bin/env sh
set -eu

ROOT="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"

cd "$ROOT"
mvn -q compile

exec java \
  -cp "target/classes" \
  io.github.protasm.jvmud.compiler.formatter.LPCFormatterCli "$@"
