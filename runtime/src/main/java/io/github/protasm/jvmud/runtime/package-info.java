/**
 * Engine-owned JVMud world primitives.
 *
 * <p>This package models the runtime concepts that belong to the engine itself: worlds, places,
 * links, entities, containment, capabilities, deterministic scheduling, and mudlib boundary
 * metadata. Compiler and LPC compatibility layers adapt into these concepts instead of replacing
 * them with legacy driver vocabulary.</p>
 */
package io.github.protasm.jvmud.runtime;
