#!/usr/bin/env sh
set -eu
JVMUD_SCRIPT_DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
. "$JVMUD_SCRIPT_DIR/runtime.sh"
export JVMUD_INSTALL_ROOT="$JVMUD_ROOT"
exec "$JVMUD_JAVA" -cp "$JVMUD_ROOT/lib/*" io.github.protasm.jvmud.transport.telnet.TelnetServer "$@"
