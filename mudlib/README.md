# JVMud Mudlib

This directory is reserved for LPC mudlib source, examples, fixtures, and other
content that will be compiled and loaded by JVMud.

No mudlib layout has been established here yet. Keep compiler implementation
work in `compiler/` and use this directory for LPC-world content when that
structure is introduced.
