=== Upstream Source Policy

Preserve upstream material and provenance. Prefer dedicated shims and manifest
mappings to broad source rewrites. Keep necessary source fixes small and tested;
a content bug is not automatically a compiler defect. Never commit private saves
or secrets. Use `database.password_env` for database credentials.
