== Troubleshooting

Begin with the smallest failing step: Java build, manifest validation, object
load, listener startup, login, or command handling. Keep the first diagnostic and
the exact manifest path; later errors may be consequences of the first failure.
